# Deferred Items

## Plan 05-04

- `cd src-tauri && cargo test` currently stops at the pre-existing Plan 05-06 scaffold `tests/daily_activity_rebuild.rs::daily_activity_rebuild_is_idempotent_and_emits_event`. The Plan 05-04 target tests and parser unit tests pass; this scaffold is intentionally owned by a later phase plan.
