---
phase: 05-project-detail-global-sessions-charts
plan: 07
subsystem: backend
tags: [rust, tauri, sqlite, sessions, charts]

requires:
  - phase: 05-project-detail-global-sessions-charts
    provides: Phase 5 schema, backend scaffolds, Project Detail commands, and portfolio heatmap storage from prior plans
provides:
  - Filtered and paged Global Sessions backend command
  - Filter-aligned Global Sessions chart aggregate command
  - Registered Global Sessions and portfolio heatmap Tauri command surfaces
affects: [global-sessions, frontend-ipc, charts, portfolio-heatmap]

tech-stack:
  added: []
  patterns: [fixed SQL predicate fragments with bound filter values, thin Tauri command wrappers, generated Tauri permissions]

key-files:
  created:
    - src-tauri/src/sessions/global.rs
    - src-tauri/permissions/autogenerated/get_global_chart_data.toml
    - src-tauri/permissions/autogenerated/get_portfolio_heatmap.toml
    - src-tauri/permissions/autogenerated/list_global_sessions.toml
  modified:
    - src-tauri/src/commands/sessions.rs
    - src-tauri/src/sessions/mod.rs
    - src-tauri/src/lib.rs
    - src-tauri/src/main.rs
    - src-tauri/build.rs
    - src-tauri/capabilities/default.json
    - src-tauri/tests/global_sessions_query.rs
    - src-tauri/tests/global_sessions_unmatched.rs
    - src-tauri/tests/global_chart_data.rs

key-decisions:
  - "Global Sessions SQL lives in src-tauri/src/sessions/global.rs to keep existing repository files under the AGENTS.md 500-line limit."
  - "Global filters append only active fixed SQL fragments and bind all user-controlled values."
  - "Unmatched-only queries add project_id IS NULL and use the existing unmatched partial index via an explicit index hint."

patterns-established:
  - "Global table and charts share the same GlobalSessionFilters DTO."
  - "Chart token totals use the D-16 input + output + cache_read + cache_creation formula."

requirements-completed: [GLOB-01, GLOB-02, GLOB-03, PORT-05]

duration: 14min
completed: 2026-04-27
---

# Phase 05 Plan 07: Global Sessions Backend Commands Summary

**Global Sessions now has safe server-side filtering, paged session rows, filter-aligned chart aggregates, and release-registered Tauri command access.**

## Performance

- **Duration:** 14 min
- **Started:** 2026-04-27T14:41:15Z
- **Completed:** 2026-04-27T14:55:37Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- Replaced the Plan 05-02 global backend scaffolds with behavior tests for combined filters, injection rejection, unmatched-only semantics, partial-index verification, and chart aggregates.
- Added `list_global_sessions` with default page size 100, max page size 200, fixed `started_at DESC NULLS LAST` ordering, and bound filter values.
- Added `get_global_chart_data` for sessions/day by source, tokens/day by top projects plus Other, 24-hour histogram, and 7-day distribution.
- Registered `list_global_sessions`, `get_global_chart_data`, and `get_portfolio_heatmap` for dev handlers, build metadata, generated permissions, and release capabilities.

## Task Commits

1. **Task 1 RED: Global sessions backend behavior tests** - `455e866` (test)
2. **Task 1 GREEN: Global sessions backend commands** - `3e7690f` (feat)
3. **Task 2: Register global sessions and heatmap commands** - `3646792` (feat)

## Files Created/Modified

- `src-tauri/src/sessions/global.rs` - Global filter DTOs, paged table rows, chart DTOs, SQL fragment builder, and aggregate queries.
- `src-tauri/src/commands/sessions.rs` - Thin Tauri command wrappers and app helpers for Global Sessions.
- `src-tauri/src/sessions/mod.rs` - Exposes the new global sessions module.
- `src-tauri/src/main.rs`, `src-tauri/src/lib.rs`, `src-tauri/build.rs`, `src-tauri/capabilities/default.json`, `src-tauri/permissions/autogenerated/*.toml` - Command registration and release permissions.
- `src-tauri/tests/global_sessions_query.rs`, `src-tauri/tests/global_sessions_unmatched.rs`, `src-tauri/tests/global_chart_data.rs` - RED/GREEN tests for GLOB-01, GLOB-02, and GLOB-03.

## Decisions Made

Global query implementation was placed in a focused `sessions/global.rs` module instead of expanding `sessions/repo.rs`, which was already near the project file-size limit. The command layer remains thin and mirrors existing Phase 5 Project Detail patterns.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Split global SQL into a focused module**
- **Found during:** Task 1 (Global Sessions implementation)
- **Issue:** Adding the global table and chart query helpers directly to `src-tauri/src/sessions/repo.rs` would push it past the AGENTS.md 500-line file limit.
- **Fix:** Added `src-tauri/src/sessions/global.rs` and exposed it through `sessions/mod.rs`.
- **Files modified:** `src-tauri/src/sessions/global.rs`, `src-tauri/src/sessions/mod.rs`, `src-tauri/src/commands/sessions.rs`
- **Verification:** `wc -l` keeps `sessions/repo.rs` at 470 lines and `sessions/global.rs` at 466 lines; plan tests pass.
- **Committed in:** `3e7690f`

**2. [Rule 1 - Bug] Corrected D-16 token-total test expectation**
- **Found during:** Task 1 GREEN verification
- **Issue:** The new chart test expected `224` tokens but the fixture included cache tokens, making the D-16 total `233`.
- **Fix:** Updated the expected aggregate to include cache_read and cache_creation tokens.
- **Files modified:** `src-tauri/tests/global_chart_data.rs`
- **Verification:** `cargo test --test global_chart_data` passed as part of plan verification.
- **Committed in:** `3e7690f`

**3. [Rule 2 - Missing Critical] Registered commands in the runtime handler**
- **Found during:** Task 2 (command registration)
- **Issue:** The plan named `lib.rs`, `build.rs`, and capabilities, but Tauri commands also need `main.rs` handler registration to be callable in the running app.
- **Fix:** Added `get_global_chart_data`, `list_global_sessions`, and `get_portfolio_heatmap` to `tauri::generate_handler!`.
- **Files modified:** `src-tauri/src/main.rs`
- **Verification:** `cd src-tauri && cargo build` passed.
- **Committed in:** `3646792`

---

**Total deviations:** 3 auto-fixed (Rule 1: 1, Rule 2: 2)
**Impact on plan:** All deviations were required for correctness or project constraints; no user-facing scope was added.

## Issues Encountered

SQLite preferred the broader `(project_id, started_at)` index for an unmatched-only EXPLAIN probe. The global unmatched query now uses the existing partial unmatched index explicitly when `unmatchedOnly` is active, and the test verifies that index path.

## Known Stubs

None introduced by this plan.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: global-session-filter-sql | `src-tauri/src/sessions/global.rs` | New user-controlled GlobalSessionFilters cross into SQL predicates; source is validated and all values are bound. |
| threat_flag: release-command-capabilities | `src-tauri/capabilities/default.json` | New release command surface for global sessions and heatmap commands is explicitly allow-listed. |

## TDD Gate Compliance

- RED commit present: `455e866`
- GREEN commit present after RED: `3e7690f`
- No separate refactor commit was needed.

## User Setup Required

None - no external service configuration required.

## Verification

- `cd src-tauri && cargo test --test global_sessions_query --test global_sessions_unmatched --test global_chart_data && cargo build` - passed.
- `grep -nE 'pub async fn (list_global_sessions|get_global_chart_data)' src-tauri/src/commands/sessions.rs` - returned both command functions.
- `grep -nE 'project_id IS NULL' src-tauri/src/sessions/repo.rs src-tauri/src/sessions/global.rs` - returned matches.
- `grep -nE 'list_global_sessions|get_global_chart_data|get_portfolio_heatmap' src-tauri/src/lib.rs src-tauri/capabilities/default.json` - returned matches in both files.

## Next Phase Readiness

Plans 05-10 and 05-11 can consume the Global Sessions table and chart DTOs. Plan 05-12 can call the already-registered portfolio heatmap command.

## Self-Check: PASSED

- Confirmed summary and key created files exist.
- Confirmed task commits exist: `455e866`, `3e7690f`, `3646792`.
- Confirmed plan verification and acceptance gates passed.

---
*Phase: 05-project-detail-global-sessions-charts*
*Completed: 2026-04-27*
