---
phase: 04-session-indexer
plan: 03
subsystem: sessions
tags: [rust, tauri, sqlite, sessions, ipc, capabilities]

requires:
  - phase: 04-session-indexer
    provides: session schema, parser, streaming offsets, project attribution
provides:
  - Tauri index_sessions command for fixed Claude and Codex session roots
  - Metadata-only SessionIndexEvent progress stream
  - Async session indexing orchestration with spawn_blocking file work and deadpool SQLite writes
  - Rebuild-cache session rematch hook after project refresh
  - Release capability and generated permission for index_sessions
affects: [phase-04-session-indexer, portfolio-session-aggregates, frontend-session-trigger]

tech-stack:
  added: []
  patterns: [fixed-root backend indexing, metadata-only progress events, transactional per-file persistence, release-strict command permissions]

key-files:
  created:
    - src-tauri/src/commands/sessions.rs
    - src-tauri/permissions/autogenerated/index_sessions.toml
  modified:
    - src-tauri/src/events.rs
    - src-tauri/src/commands/scan.rs
    - src-tauri/src/sessions/indexer.rs
    - src-tauri/src/commands/mod.rs
    - src-tauri/src/main.rs
    - src-tauri/build.rs
    - src-tauri/capabilities/default.json
    - src-tauri/tests/session_indexer.rs
    - src-tauri/tests/rebuild_cache.rs

key-decisions:
  - "index_sessions accepts no frontend path input; it derives only $HOME/.claude/projects and $HOME/.codex/sessions from AppState."
  - "SessionIndexEvent emits source, path, counts, status, and error text only; prompt/message bodies never cross IPC."
  - "Rebuild cache rematches existing unmatched sessions after refreshed project rows are visible, preserving session rows and offsets."

patterns-established:
  - "index_session_roots wraps sync file discovery and JSONL streaming in spawn_blocking, while all SQLite access uses deadpool interact."
  - "Command orchestration calls persist_indexed_file_result for per-file writes and never calls save_index_state directly."
  - "Tauri command registration requires build.rs metadata, default capability, and autogenerated permission TOML together."

requirements-completed: [SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, SESS-06]

duration: 7min
completed: 2026-04-26
---

# Phase 04 Plan 03: Session Index Command Summary

**Fixed-root Claude/Codex session indexing command with metadata-only progress events and release permissions**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-26T12:14:59Z
- **Completed:** 2026-04-26T12:21:51Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- Added red command-level coverage for fixture-root indexing, incremental offset reuse, rollback after persistence failure, rebuild-cache rematching, and release registration.
- Implemented `index_sessions` / `index_sessions_for_app` over fixed Claude and Codex roots with `SessionIndexEvent` progress and transactional per-file persistence.
- Registered the command in Tauri handler/build metadata and release capabilities without granting frontend filesystem read scope.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add command-level indexer coverage** - `767280b` (test)
2. **Task 2: Implement session indexing orchestration and progress events** - `9ed86eb` (feat)
3. **Task 3: Register command metadata and release capability** - `300702b` (feat)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `src-tauri/src/commands/sessions.rs` - Thin Tauri command plus testable `index_sessions_for_app` helper.
- `src-tauri/src/events.rs` - Added tagged/camelCase `SessionIndexEvent`.
- `src-tauri/src/sessions/indexer.rs` - Added root discovery, recursive JSONL discovery, async orchestration, project matching, and per-file persistence calls.
- `src-tauri/src/commands/scan.rs` - Rematches preserved session rows after rebuild-cache project refresh.
- `src-tauri/src/commands/mod.rs` - Exports the sessions command module.
- `src-tauri/src/main.rs` - Registers `index_sessions` with `generate_handler!`.
- `src-tauri/build.rs` - Adds `index_sessions` to Tauri command metadata.
- `src-tauri/capabilities/default.json` - Allows `allow-index-sessions` only.
- `src-tauri/permissions/autogenerated/index_sessions.toml` - Generated allow/deny permission metadata for the command.
- `src-tauri/tests/session_indexer.rs` - Adds command orchestration, offset, failure, and release registration tests.
- `src-tauri/tests/rebuild_cache.rs` - Adds rebuild-cache rematch regression coverage.

## Decisions Made

- Kept indexing roots backend-owned and fixed to `AppState.home_dir`, avoiding arbitrary path IPC input.
- Counted missing Claude/Codex roots as skipped rather than errors, so machines with only one tool installed still index successfully.
- Included the generated Tauri permission TOML in the Task 3 commit because release capability references require the command permission artifact.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added generated Tauri permission artifact**
- **Found during:** Task 3 (Register command metadata and release capability)
- **Issue:** Running the Tauri command metadata build generated `src-tauri/permissions/autogenerated/index_sessions.toml`, which is required for the new `allow-index-sessions` permission but was not listed in expected files.
- **Fix:** Committed the generated allow/deny permission TOML with the command metadata and default capability changes.
- **Files modified:** `src-tauri/permissions/autogenerated/index_sessions.toml`
- **Verification:** `cargo test --manifest-path src-tauri/Cargo.toml --test session_indexer -- --nocapture`
- **Committed in:** `300702b`

**Total deviations:** 1 auto-fixed (1 blocking issue)
**Impact on plan:** Required for release-mode command permission correctness; no frontend filesystem scope was added.

## Issues Encountered

- The literal plan command `cargo test --manifest-path src-tauri/Cargo.toml session_indexer -- --nocapture` compiles but runs zero tests because Cargo treats `session_indexer` as a test-name filter. The actual integration target was also run with `cargo test --manifest-path src-tauri/Cargo.toml --test session_indexer -- --nocapture`, which passed 10 tests.
- The red Task 1 release registration test necessarily failed until Task 3. Task 2 was verified in the combined working tree, then staged and committed separately from Task 3 metadata to preserve atomic commits.

## Verification

- `cargo test --manifest-path src-tauri/Cargo.toml session_indexer -- --nocapture` - passed, but filtered out all tests.
- `cargo test --manifest-path src-tauri/Cargo.toml rebuild_cache_rematches_existing_sessions_after_project_roots_refresh -- --nocapture` - 1 passed.
- `cargo test --manifest-path src-tauri/Cargo.toml --test session_indexer -- --nocapture` - 10 passed.
- Acceptance grep checks passed for command tests, `SessionIndexEvent`, command helpers, `persist_indexed_file_result`, rebuild rematch hook, no direct `save_index_state` orchestration call, `spawn_blocking`, fixed roots, command registration, capability allow, and no `fs:allow-read`.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 04-04 can wire the frontend trigger and portfolio aggregates to `index_sessions`; backend indexing, progress events, offset safety, rematch behavior, and release permissions are ready.

## Self-Check: PASSED

- Verified created files `src-tauri/src/commands/sessions.rs`, `src-tauri/permissions/autogenerated/index_sessions.toml`, and `.planning/phases/04-session-indexer/04-03-SUMMARY.md` exist.
- Verified task commits `767280b`, `9ed86eb`, and `300702b` exist in git history.

---
*Phase: 04-session-indexer*
*Completed: 2026-04-26*
