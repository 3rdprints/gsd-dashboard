---
phase: 07-live-updates
plan: 02
subsystem: backend
tags: [rust, tauri, watcher, notify, live-updates]
requires:
  - phase: 07-live-updates
    provides: Phase 07 watcher and frontend validation scaffolds
provides:
  - Stable watcher root/status DTO contracts
  - Runtime-owned watcher status on AppState
  - get_watcher_status backend command and release permission
  - Tiny live update AppEvent variants
affects: [07-live-updates, watcher, settings, app-events]
tech-stack:
  added: [notify 8.2.0, notify-debouncer-full 0.5.0]
  patterns: [runtime-only watcher status, narrow derived watch roots, tiny invalidation events]
key-files:
  created:
    - src-tauri/src/watcher/mod.rs
    - src-tauri/src/watcher/roots.rs
    - src-tauri/src/watcher/service.rs
    - src-tauri/permissions/autogenerated/get_watcher_status.toml
  modified:
    - src-tauri/Cargo.toml
    - src-tauri/Cargo.lock
    - src-tauri/src/app_state.rs
    - src-tauri/src/bootstrap.rs
    - src-tauri/src/commands/settings.rs
    - src-tauri/src/events.rs
    - src-tauri/src/main.rs
    - src-tauri/capabilities/default.json
    - src-tauri/tests/bootstrap.rs
    - src-tauri/tests/live_updates_watcher.rs
    - src-tauri/tests/settings_commands.rs
key-decisions:
  - "Watcher status is runtime-only AppState data and is not persisted in Settings."
  - "07-02 starts watcher ownership during bootstrap but only initializes root status contracts; native OS watch registration remains for later Phase 07 plans."
  - "Live update events carry only IDs or a no-payload invalidator."
patterns-established:
  - "derive_watcher_roots returns discovered project .planning directories plus existing ~/.claude/projects and ~/.codex/sessions roots only."
  - "Watcher fallback status normalizes platform errors into permission, watchLimit, filesystem, or unknown UI-safe categories."
requirements-completed: [LIVE-01, LIVE-03, LIVE-04, LIVE-05]
duration: 7min
completed: 2026-05-01
---

# Phase 07 Plan 02: Watcher Backend Contracts Summary

**Runtime watcher root/status contracts with stable notify pins, queryable Settings status, and tiny live-update events**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-01T18:59:30Z
- **Completed:** 2026-05-01T19:06:29Z
- **Tasks:** 2
- **Files modified:** 16

## Accomplishments

- Added `notify = "8.2"` and `notify-debouncer-full = "0.5"` with lockfile updates; no `notify` 9.0 RC dependency was introduced.
- Created the `watcher` backend module with root derivation, constants, runtime status storage, fallback reason normalization, and startup ownership.
- Added `get_watcher_status` as a release-capable Tauri command returning runtime status only.
- Added typed tiny live update events: `project:updated`, `session:new`, and `watcher:status-changed`.

## Task Commits

1. **Task 1 RED: Add failing watcher contract tests** - `147395c` (test)
2. **Task 1 GREEN: Add watcher root and status contracts** - `0d4292b` (feat)
3. **Task 2 RED: Add failing watcher command and event tests** - `7b1023f` (test)
4. **Task 2 GREEN: Wire watcher status command and live events** - `a3ccaff` (feat)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `src-tauri/src/watcher/mod.rs` - Public watcher module exports.
- `src-tauri/src/watcher/roots.rs` - Narrow root derivation and scan-root polling guardrail reuse.
- `src-tauri/src/watcher/service.rs` - Runtime status DTOs, constants, fallback normalization, and bootstrap startup hook.
- `src-tauri/src/app_state.rs` - Adds `WatcherRuntime` ownership.
- `src-tauri/src/bootstrap.rs` - Starts watcher runtime ownership after cache/settings bootstrap.
- `src-tauri/src/commands/settings.rs` - Adds `get_watcher_status`.
- `src-tauri/src/events.rs` - Adds tiny live update event variants.
- `src-tauri/src/main.rs`, `src-tauri/capabilities/default.json`, `src-tauri/permissions/autogenerated/get_watcher_status.toml` - Registers the new command for dev and release.
- `src-tauri/tests/live_updates_watcher.rs`, `src-tauri/tests/settings_commands.rs`, `src-tauri/tests/bootstrap.rs` - Active TDD coverage for watcher contracts, command status, and event serialization.

## Decisions Made

- Used Context7 for current `notify`/`notify-debouncer-full` API documentation before adding watcher dependencies.
- Kept OS-native watcher registration out of this plan because 07-02 explicitly establishes contracts and startup ownership before targeted refresh/native watching implementation.
- Returned UI-safe fallback copy from backend status instead of raw OS errors.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The original Phase 07 scaffold tests were ignored. Converted Task 1 coverage into active TDD gates so the RED/GREEN flow verified real contracts.
- `gsd-sdk query state.record-metric` rejected both positional and documented named arguments in this repository state, so the `Plan 07-02` metric line was added to `STATE.md` manually after other SDK state updates succeeded.

## Known Stubs

None. `start_watcher_service` intentionally initializes runtime status ownership only; native watch registration is planned for later Phase 07 implementation.

## Verification

- `cargo test --manifest-path src-tauri/Cargo.toml live_updates_watcher -- --nocapture` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml settings_commands -- --nocapture` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml events -- --nocapture` passed.
- Grep gates passed for stable watcher crate pins, `.codex/sessions` root scope, no `notify` 9.0 dependency, no `archived_sessions`/`CODEX_HOME` watcher references, command permission registration, and no raw transcript/prompt/tool-output/planning-body event fields.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Later Phase 07 plans can build targeted refresh and frontend rendering against stable watcher roots, fallback status DTOs, release IPC permission, and tiny invalidation event names.

## Self-Check: PASSED

- Found summary file, watcher module files, and `get_watcher_status` permission file.
- Found task commits `147395c`, `0d4292b`, `7b1023f`, and `a3ccaff` in git history.

---
*Phase: 07-live-updates*
*Completed: 2026-05-01*
