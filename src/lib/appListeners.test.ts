import { describe, it } from "vitest";

describe("live update app listeners", () => {
  it.todo("invalidates project queries from project:updated id payload");
  it.todo("invalidates global and project session queries from session:new tiny payload");
  it.todo("invalidates watcher status from watcher:status-changed");
});
